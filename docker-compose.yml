version: '3.8'

services:
  teledrive:
    build: .
    container_name: teledrive
    restart: unless-stopped
    ports:
      - "3050:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      # Persistent SQLite Database, .env Config, Cache & Session Storage
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/setup/status"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
